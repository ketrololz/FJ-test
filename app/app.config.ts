export default defineAppConfig({
  ui: {
    colors: {
      primary: "amber",
      secondary: "brand",
      neutral: "zinc",
    },
    container: {
      base: "w-full max-w-(--ui-container) mx-auto",
    },
  },
});
